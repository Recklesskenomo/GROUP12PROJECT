# =============================================================================
# Courier & Logistics System — Database Configuration
# =============================================================================
# Copy this file to db.properties and fill in your MySQL credentials.
# This file is in .gitignore — do NOT commit credentials to Git.
# =============================================================================

db.url=jdbc:mysql://localhost:3306/courier_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
db.username=root
db.password=
